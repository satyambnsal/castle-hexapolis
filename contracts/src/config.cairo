// ----------------------------------------------------------------------------
// Constants
// ----------------------------------------------------------------------------

const INITIAL_MOVES: u8 = 25;
const X_RANGE: u128 = 50; // These need to be u128
const Y_RANGE: u128 = 50; // These need to be u128
const Z_RANGE: u128 = 50; // These need to be u128

const ORIGIN_OFFSET: u8 = 100; // Origin offset so no overflow
const MAP_AMPLITUDE: u128 = 16; // The lower the value, the highest variation in the map
